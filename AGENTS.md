# 数学读书笔记库 · AGENTS.md

> 本文件是本 Obsidian vault 的「AI 员工手册」（Pi 优先读 AGENTS.md）。
> 本库**只用来放数学读书笔记**：线性代数、分析、几何、代数、概率等。

## 关于本库

- 主题：数学读书笔记与概念整理。
- 语言：中文。**wiki 文件名与索引一律用中文**，让 Obsidian 图谱显示中文。
- 偏好：保留原始素材；可补充标签、摘要、双向链接，但**未经确认不得删除或改写正文**。

## 目录结构

```
0_Note/
├── raw/                 # 原始素材（只增不改，source of truth）
│   ├── 素材索引.md
│   └── 学科/分支/        # 如 raw/数学/线性代数/线性代数应该这样学-伴读.md
├── wiki/                # 结构化知识（中文命名）
│   ├── 知识索引.md
│   ├── 概念/            # 概念页（含 概念索引.md）
│   ├── 实体/            # 人物、书籍（含 实体索引.md）
│   └── 主题/            # 跨概念综述（含 主题索引.md）
├── output/              # 产出报告（含 产出索引.md）
├── templates/           # 笔记模板
├── 总索引.md            # 全局导航索引
└── AGENTS.md            # 本文件
```

- 图片等附件放在笔记同级目录，用 Obsidian 嵌入语法 `![[图片.png]]`。
- **索引文件用领域相关的中文名**：`总索引.md`、`素材索引.md`、`知识索引.md`、`概念索引.md`、`实体索引.md`、`主题索引.md`、`产出索引.md`。
- **不写日记**，不使用 Daily Notes。

## Frontmatter 规范（五字段）

除 `AGENTS.md` / `索引.md` / `templates/` 外，每条笔记都必须包含：

```yaml
---
title: 笔记标题
tags: [领域标签, 具体主题]
created: YYYY-MM-DD
type: literature
summary: 一句话摘要（30–80 字）
---
```

`type` 只允许三个值：

- `fleeting`：闪念、待整理素材。
- `literature`：读书/文献笔记（本库 raw 与多数笔记）。
- `permanent`：经过思考、属于你自己的总结（wiki 概念页推荐）。

## 命名规范

- **wiki 文件用中文**：`wiki/概念/向量空间.md`、`wiki/实体/线性代数应该这样学.md`。
- **索引文件用领域相关的中文名**：`总索引.md`、`素材索引.md`、`知识索引.md`、`概念索引.md`、`实体索引.md`、`主题索引.md`、`产出索引.md`。
- raw 文件名用「内容 + 章节」：`第01章-向量空间.md`。
- 概念页一个概念一个文件，避免同义词分裂（统一用词见下）。

## 标签规范

- 领域标签：`数学`、`线性代数`、`实分析`、`复分析`、`几何`、`拓扑`、`代数`、`概率统计`。
- 主题标签：`向量空间`、`线性映射`、`本征值`、`内积`、`谱定理`、`若尔当形`、`行列式` 等。
- 状态标签：`待整理`、`进行中`、`已完成`。结构标签：`索引`。
- 领域标签保持克制，不要自创；新增先更新本文件。

## 术语统一

- 同一概念只用一个写法，例如：`本征值`（不用“特征值”混用，除非书名原文）、`向量空间`、`线性映射`、`若尔当形`。
- 中英对照写在概念页里，不改变正文用词。

## 工作流（raw → wiki → output）

1. **整理 raw**：把外部素材（对话导出、网页、PDF 摘录）转成 Markdown 放入 `raw/学科/分支/书籍/`，去重、按内容命名。
2. **编译 wiki**：读取 raw，按概念建立 `wiki/概念/` 页面（中文名），在 `wiki/实体/` 建人物/书籍页，必要时建 `wiki/主题/`；用 `[[双向链接]]` 互连；更新各层索引（`知识索引.md`、`概念索引.md` 等）。
3. **产出 output**：基于 wiki 生成章节地图、速查表、学习路线等报告；好的产出可反哺 wiki。
4. **记录来源**：每个 wiki 页面在「参考源」列出对应 raw 文件。

## 行为规则

- ✅ 可以：补充 frontmatter、创建 `[[双向链接]]`、生成/更新索引（`总索引.md`、`知识索引.md`、`概念索引.md` 等）、把 raw 编译进 wiki。
- ❌ 不可以：删除已有笔记正文、批量重命名（除非明确被要求）、新建顶层目录（除非先更新本文件）。
- 处理 `.md` 时使用 Obsidian 语法：双链 `[[笔记名]]`、嵌入 `![[图片.png]]`、Callout `> [!note]`。
- 每次整理后更新相关索引（`总索引.md`、`知识索引.md`、`概念索引.md` 等）。

## Skills

- 项目技能位于 `.pi/skills/`（Pi 自动发现）。其中 `build-wiki` 固化了「raw → 中文 wiki → output」流程。
